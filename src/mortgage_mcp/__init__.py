"""
MCP references: (package root — imported by application entrypoints, not directly by MCP clients)
Purpose: Marks `mortgage_mcp` as a package and exposes the public version string.
"""

__version__ = "0.1.0"
