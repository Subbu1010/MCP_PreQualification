# MCP references: (Streamlit UI — installs client deps for all tool pages)
# Purpose: Simple pip install on a separate server without editable package metadata.
streamlit>=1.40.0
mcp>=1.21.0
python-dotenv>=1.0.0
