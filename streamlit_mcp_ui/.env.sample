# MCP references: (Streamlit UI — configures remote MCP Streamable HTTP URL)
# Purpose: Copy to `.env` or set in the shell / container before `streamlit run`.
MCP_SERVER_URL=http://127.0.0.1:8080/mcp
